from __future__ import annotations
from typing import TYPE_CHECKING

from {{ settings.primary_module_qualified | default(settings.primary_module) }}.main import main

if TYPE_CHECKING:
    from click.testing import CliRunner
    from pytest_mock import MockerFixture


def test_main(runner: CliRunner, mocker: MockerFixture) -> None:
    """Test main function."""
    # mocker.patch('{{ settings.primary_module_qualified | default(settings.primary_module) }}.main.something', return_value=True)
    result = runner.invoke(main)
    assert result.exit_code == 0
